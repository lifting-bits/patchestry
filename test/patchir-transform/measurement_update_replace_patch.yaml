# Test patching specification
apiVersion: patchestry.io/v1

metadata:
  name: "measurement-update-replace-patch"
  description: "Patch measurement update replace patch"
  version: "1.0.0"
  author: "Security Team"
  created: "2025-07-28"

libraries:
  - "patches/measurement_update_patch.yaml"

target:
  binary: "test_binary.bin"
  arch: "ARM:LE:32"

patches:
  - name: "measurement_update_replace_patch"
    id: "MEASUREMENT-UPDATE-REPLACE-PATCH-001"
    description: "Patch measurement update replace patch"
    match:
      name: "spo2_lookup"
      kind: "function"
    mode: "replace"
    patch: "measurement_update_replace_patch"
    arguments:
      - name: "function_argument"
        source: "operand"
        index: 0
