apiVersion: patchestry.io/v1
metadata:
  name: stray-target-loader
  kind: PatchSpec
target:
  binary: test_binary.bin
  arch: ARM:LE:32
libraries:
- stray_target_in_library_reject.yaml
patches:
- name: irrelevant
  id: IRRELEVANT-002
  match:
    name: minimal_func
    kind: function
  action:
    mode: apply_before
    patch: dummy
