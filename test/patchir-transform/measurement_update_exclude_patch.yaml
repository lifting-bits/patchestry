# Test patching specification
apiVersion: patchestry.io/v1

metadata:
  name: "measurement-update-exclude-patch"
  description: "Patch measurement update exclude patch"
  version: "1.0.0"
  author: "Security Team"
  created: "2025-07-28"

libraries:
  patches: "patches/measurement_update_patch.yaml"

target:
  binary: "test_binary.bin"
  arch: "ARM:LE:32"

meta_patches:
  - name: "measurement_update_exclude_patch"
    id: "MEASUREMENT-UPDATE-EXCLUDE-PATCH-001"
    description: "Patch measurement update exclude patch"

    patch_actions:
      - id: "MEASUREMENT-UPDATE-EXCLUDE-PATCH-001"
        description: "Patch measurement update exclude patch"

        # Match Configuration
        match:
          - name: "spo2_lookup"
            kind: "function"
            argument_matches:
              - index: 0
                name: "var6"
                type: "!cir.float"

        # Actions to Apply
        action:
          - mode: "apply_before"
            patch_id: "MEASUREMENT-UPDATE-BEFORE-PATCH-001"
            description: "Patch measurement update exclude patch"

            # Arguments
            arguments:
              - name: "function_argument"
                source: "variable"
                symbol: "var6"

    exclude:
      - "measurement_update"
