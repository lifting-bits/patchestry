apiVersion: patchestry.io/v1
metadata:
  name: erase-sprintf-with-return
  description: Remove sprintf() call whose return is consumed
  kind: PatchSpec
target:
  binary: test_binary.bin
  arch: ARM:LE:32
patches:
- name: strip_sprintf
  id: ERASE-SPRINTF-001
  match:
    name: sprintf
    kind: function
    context:
    - create_port
  action:
    mode: erase
