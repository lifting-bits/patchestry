apiVersion: patchestry.io/v1
metadata:
  name: dup-deployment
  kind: PatchSpec
target:
  binary: test_binary.bin
  arch: ARM:LE:32
libraries:
- duplicate_definition_lib_a.yaml
- duplicate_definition_lib_b.yaml
patches:
- name: irrelevant
  id: IRRELEVANT-003
  match:
    name: minimal_func
    kind: function
  action:
    mode: apply_before
    patch: dup_patch
