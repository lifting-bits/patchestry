apiVersion: patchestry.io/v1
metadata:
  name: dup-within-deployment
  kind: PatchSpec
target:
  binary: test_binary.bin
  arch: ARM:LE:32
libraries:
- duplicate_within_lib.yaml
patches:
- name: irrelevant
  id: IRRELEVANT-004
  match:
    name: minimal_func
    kind: function
  action:
    mode: apply_before
    patch: dup_patch
